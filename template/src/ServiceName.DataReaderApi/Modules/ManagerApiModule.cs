using Autofac;

namespace ServiceName.DataReaderApi.Modules
{
    public class ManagerApiModule : Module
    {
        public ManagerApiModule()
        {
        }

        protected override void Load(ContainerBuilder builder)
        {
            base.Load(builder);
        }
    }
}
